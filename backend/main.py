# This file is a stub — the real backend is /main.py at the project root.
# Run from the project root: uvicorn main:app --reload --port 8000
raise RuntimeError(
    "Wrong entry point! Run 'uvicorn main:app --reload --port 8000' from the project root, not from backend/"
)